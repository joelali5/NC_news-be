# Creating Environment Variables
Create two files for the environment variables `.env.development` and `.env.test`
Into each of these, add PGDATABASE=<database_name_here>, with the correct database name for that environment.